# Day 6

**1.Movie class**
1. I created a class and named as Movie.
2. Then, I created constructor for the class Movie, which takes a String representing the title of the movie, a String representing the studio, and a String representing the rating as its arguments.
3. And seted the respective class properties to these values.
4. This constructor for the class Movie will set the class property rating to "PG" as default when no rating is provided.
5. And I created a method getPG, which takes an array of base type Movie as its argument, and returns a new array of only those movies in the input array with a rating of "PG".
6. And I created an instance of the class Movie with the title "Casino Royale", the studio "Eon Productions", and the rating "PG­13".
7. Then I completed in this [Movie Class File](/movieclass.js)
   
**2.Circle class**
1. I created a class and named as Circle.
2. Then, I created constructor for the class Circle, and added Instance variable as radius of the circle which set a default value of 1.0 and color representing the color of circle which set a default value of "red" as its arguments.
3. Then, I created a method named toString.
4. And I created getter methods such as getRadius, getColor, getArea and getCircumference.
5. And I created setter methods such as setRadius and setColor.
6. The getArea method will return the Area of circle with radius.
7. The getCircumference method will return the Circumference of circle with radius.
8. Then I completed in this [Circle Class File](/circleclass.js)

**3. Person class**
1. I created a class and named as Person.
2. Then, I created constructor for the class Person, and added Instance variable such as Name, Age, Place, Phone number and Email id.
3. The Person class holds all the details.
4. Then I completed in this [Person Class File](/personclass.js)

**4.Uber price class**
1. I created a class and named as Uber.
2. Then, I created constructor for the class Uber, which takes a distance representing the distance of the Uber travel.
3. I consider the cost of uber for 1km is 5.
4. The result will return the price of the traveled distance.
5. I completed in this [Uber Class File](/uberclass.js)